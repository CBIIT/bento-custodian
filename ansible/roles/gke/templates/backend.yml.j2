kind: Service
apiVersion: v1
metadata:
  name: bento-backend-service
spec:
  type: NodePort
  selector:
    app: bento
    role: backend
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http