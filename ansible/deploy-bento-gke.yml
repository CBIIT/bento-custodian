---
- name: deploy stage of bento pipeline
  hosts: all
  connection: local
  gather_facts: yes
    
  tasks:
    - name: deploy stage
      include_role:
        name: gke
        tasks_from: deploy
      